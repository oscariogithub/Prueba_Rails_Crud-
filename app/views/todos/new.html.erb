<h1>Agrega una nueva Tarea</h1>
<%= form_with(model: @todo, local: true) do |f| %>

        <%= f.label :description, "Descripciòn" %>
        <%= f.text_field :description %>

        <br>

        <%= f.label :completed, "Completado" %>
        <%= f.check_box :completed %>
        
        <br>
        <%= f.submit "Enviar" %>

<% end %>

<br>
<%= link_to "Back", todos_path %>
